server:
  host: "0.0.0.0"
  port: 8815
  log_file: "server/logs/aws_idle_test_server.log"
  log_level: "INFO"
  allow_header_overrides: false

  delay:
    strategy: fixed
    initial_ms: 0          # Respond immediately

  grpc_options: []         # No gRPC keepalives
  tcp_settings:
    tcp_keepalive: false   # No TCP keepalives to test pure idle timeout
