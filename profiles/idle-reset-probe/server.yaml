server:
  host: "0.0.0.0"
  port: 8815
  log_file: "server/logs/idle_reset_probe_server.log"
  log_level: "INFO"
  allow_header_overrides: false

  delay:
    strategy: fixed
    initial_ms: 0

  grpc_options: []
  tcp_settings:
    tcp_keepalive: false
