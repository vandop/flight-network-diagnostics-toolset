server:
  host: "0.0.0.0"
  port: 8815
  log_file: "server/logs/tcp_keepalive_60s_server.log"
  log_level: "INFO"
  allow_header_overrides: false

  delay:
    strategy: multiplier
    initial_ms: 60000
    multiplier: 2.0
    max_ms: 300000

  grpc_options: []
  tcp_settings:
    tcp_keepalive: true
    tcp_keepidle: 60
    tcp_keepintvl: 10
    tcp_keepcnt: 5
